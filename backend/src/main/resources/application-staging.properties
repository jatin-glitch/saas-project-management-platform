# Staging Environment Configuration
spring.profiles.active=staging

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres-staging:5432/saas_platform_staging}
spring.datasource.username=${POSTGRES_USER:saas_user}
spring.datasource.password=${POSTGRES_PASSWORD:staging_password}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=3
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.connection-timeout=20000

# Redis Configuration
spring.redis.host=${REDIS_HOST:redis-staging}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.timeout=2000ms
spring.redis.lettuce.pool.max-active=15
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=3

# JWT Configuration
jwt.secret=${JWT_SECRET:staging-jwt-secret-key}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# Logging Configuration
logging.level.com.saas=INFO
logging.level.org.springframework.security=INFO
logging.level.org.hibernate=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=15

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.metrics.export.prometheus.enabled=true

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://staging.yourdomain.com}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=Authorization,Content-Type,X-Tenant-ID
cors.allow-credentials=true

# Rate Limiting
rate-limiting.enabled=true
rate-limiting.requests-per-minute=${RATE_LIMIT_RPM:80}

# Security Configuration
security.headers.enabled=true
security.csrf.enabled=false
security.xss-protection.enabled=true

# Performance Configuration
spring.task.execution.pool.core-size=8
spring.task.execution.pool.max-size=30
spring.task.execution.pool.queue-capacity=50

# Cache Configuration
spring.cache.type=redis
spring.cache.redis.time-to-live=300000
spring.cache.redis.cache-null-values=false
